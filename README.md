# mpmc_queue

Minimal C++20 project configured with CMake and GoogleTest.

## Build

```bash
mkdir -p build
cd build
cmake ..
cmake --build .
```

## Run

```bash
./main
```

## Test

```bash
cd build
ctest --output-on-failure
```
